#!/usr/bin/env ruby

require 'rubygems'
require 'yaml'
require 'kilt'

fork do  
  token = YAML.load_file(File.expand_path("~/.pivotal_tracker"))['token']
  
  Kilt.init token  

  loop do  
    sleep 5  
  end  
end
